version: "3.3"
services:
  web:
    build: .
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: bash -c "rm -f tmp/pids/server.pid && bundle exec rails s -p 3000 -b '0.0.0.0'"
    extra_hosts:
      - localhost:172.22.0.1

